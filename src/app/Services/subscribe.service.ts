// import { Injectable } from '@angular/core';

// @Injectable({
//   providedIn: 'root',
// })
export class SubscribeService {
  constructor() {}
  onSubscribeClicked(type: string) {
    // ADD user

    // send email

    // allow user to access

    alert('Thank you for subscribing ' + type + ' subscription');
  }
}
